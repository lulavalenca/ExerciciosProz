"""
1 - Exemplo de Execução
# Recebendo a altura do degrau e a altura desejada do usuário
altura_degrau = float(input("Digite a altura do degrau (em metros): "))
altura_desejada = float(input("Digite a altura que você deseja alcançar (em metros): "))

# Calculando o número de degraus necessários
numero_degraus = altura_desejada / altura_degrau

# Arredondando para o próximo número inteiro (pois não podemos ter meio degrau)
numero_degraus = round(numero_degraus)

# Exibindo o resultado
print(f"Você precisa subir {numero_degraus} degraus para atingir a altura desejada.")


2 - Exemplo de execução

# Receba a altura do degrau da escada
altura_degrau = float(input("Digite a altura do degrau da escada: "))

  # Receba a altura que o usuário deseja alcançar subindo a escada
altura_desejada = float(input("Digite a altura que o usuário deseja alcançar subindo a escada: "))

  # Calcule o número de degraus que o usuário deverá subir
numero_degraus = altura_desejada / altura_degrau

  # Mostre o número de degraus que o usuário deverá subir
print("O usuário deverá subir", numero_degraus, "degraus para atingir seu objetivo.")



"""

# Recebendo a altura do degrau e a altura desejada do usuário
altura_degrau = float(input("Digite a altura do degrau (em metros): "))
altura_desejada = float(input("Digite a altura que você deseja alcançar (em metros): "))

# Calculando o número de degraus necessários
numero_degraus = altura_desejada / altura_degrau

# Arredondando para o próximo número inteiro (pois não podemos ter meio degrau)
numero_degraus = round(numero_degraus)

# Exibindo o resultado
print(f"Você precisa subir {numero_degraus} degraus para atingir a altura desejada.")
