
"""
1- Exemplo

import math

# Leitura das coordenadas x e y do ponto
x = float(input("Digite a coordenada x do ponto: "))
y = float(input("Digite a coordenada y do ponto: "))

# Calculando a distância da origem (0,0)
distancia_origem = math.sqrt(x ** 2 + y ** 2)

# Imprimindo o resultado
print(f"A distância do ponto ({x}, {y}) até a origem (0,0) é: {distancia_origem}")



"""
import math

# Leitura das coordenadas x e y do ponto
x = float(input("Digite a coordenada x do ponto: "))
y = float(input("Digite a coordenada y do ponto: "))

# Calculando a distância da origem (0,0)
distancia_origem = math.sqrt(x ** 2 + y ** 2)

# Imprimindo o resultado
print(f"A distância do ponto ({x}, {y}) até a origem (0,0) é: {distancia_origem}")
